import React from "react";

// Use of destructuring to access and create a new variable of the same name:
const Protein = ({ type }) => {
  return <div>Protein: {type} </div>;
};

export default Protein;
